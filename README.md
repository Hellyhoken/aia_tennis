# aia_tennis

Python code for Hellyhoken AIA tennis bot

Requires [AIGamePyLibrary](https://github.com/theaia/AIGamePyLibrary). Can be gotten with git clone in this directory, the resulting directory is already included in `.gitignore`.
```bash
git clone git@github.com:theaia/AIGamePyLibrary.git
```